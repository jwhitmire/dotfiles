#compdef vmrun
#autoload

# vmrun zsh completion (for vmware fusion)

local ctx="$curcontext"

local vmdir arguments
vmdir=~/Documents/Virtual\ Machines.localized

arguments=(
  '-h+[host name]'
  '-T+[host type]:host_type:(ws fusion)'
)

_arguments -s $arguments
